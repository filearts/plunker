!!! 5
html
  head
    meta(charset="utf-8")
    meta(name="viewport",content="width=device-width, initial-scale=1.0")
    title Plunker - Authorized to #{auth.service}

    != css("/bootstrap/less/bootstrap")
    != js("/vendor/jquery")

    script
      if (window.opener) {
        window.opener.plunker.mediator.trigger("event:auth", !{JSON.stringify(auth)});
        window.close();
      }

  body
    div.container-fluid
      div.row
        div.span12
          h1 Authorized to #{auth.service}
          