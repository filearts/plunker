###
# Updater
#
# Process that takes json from the client, validates the format and updates
# the corresponding plunk if it exists.
#
###

module.exports = updater = []

updater.push (id, json, next) ->
  next("Not implemented")